function student(){
    var id = 12
    var name = "Isaac"
    var marks = 81
    function setMarks(){
    if(isNaN(newMarks))
    throw new Error(`${newMarks} is not a number`)
    marks = newMarks
    }
    }

let p1=student.setMarks()
console.log(p1)